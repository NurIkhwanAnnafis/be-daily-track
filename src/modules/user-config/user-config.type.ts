export type UserConfig = {
  current_amount: string
  initial_amount: string
  expense_limit_per_day: string
  expense_limit_per_month: string
  income_limit_per_day: string
  income_limit_per_month: string
}